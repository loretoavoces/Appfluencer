<div class="container influ-index">

    <div class="row justify-content-center align-items-center">
        <div class="col-4">
            <img src="{{image}}">
        </div>
        <div class="col-sm-12 col-md-6 col-lg-6">
            <h3>{{name}}</h3>
            <p>{{description}}</p>
            <p><strong>Instagram: <a class="link-detail" href="{{instagram}}">{{name}}</a></strong> | <strong>Followers:
                </strong> {{followers}}</p>
                {{#each agency}}
            <p><strong>Agencia: <a class="link-detail" href="/influencer/mapa/{{id}}">{{name}}</p></a></strong>
            {{/each}}

            <form action="/influencer/favorita/{{id}}" method="POST">
                <button type="submit" class="btn btn-sm text-white">Añadir a favoritas</button>
            </form>
            
            <hr>

            <a href="/influencer" class="btn btn-sm text-white">Volver</a>
            {{#if isAdmin}}
            <a href="/influencer/eliminar-influencer?id={{id}}" class="btn text-white">Eliminar</a>
            <a href="/influencer/editar-influencer?id={{id}}" class="btn text-white btn-sm">Editar</a>
            {{/if}}

        </div>
    </div>
</div>

{{>footer this}}
